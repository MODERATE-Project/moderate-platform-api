# Trust Service Configuration
# ==========================
# Based on upstream: https://github.com/Cybersecurity-LINKS/trust-service/blob/main/actix-server/.env
#
# This service uses LINKS Foundation's private Stardust network, which provides
# a free testnet with faucet support. The public IOTA testnet has been deprecated.

# ------------------------------------------------------------------------------
# Rust Runtime Settings
# ------------------------------------------------------------------------------

# Log level for the Rust application (trace, debug, info, warn, error)
RUST_LOG=debug

# Enable stack traces on panic (0 = disabled, 1 = enabled)
RUST_BACKTRACE=1

# ------------------------------------------------------------------------------
# Server Binding Configuration
# ------------------------------------------------------------------------------

# Network address for the DLT service to bind to
# Use "0.0.0.0" for Docker deployment, "127.0.0.1" for local development
ADDR_D=0.0.0.0

# Network address for the local service to bind to
# Use "0.0.0.0" for Docker deployment, "127.0.0.1" for local development
ADDR_L=0.0.0.0

# HTTP port the trust service listens on
PORT=8081

# ------------------------------------------------------------------------------
# IOTA Layer 1 (Tangle) Endpoints
# ------------------------------------------------------------------------------
# These endpoints connect to LINKS Foundation's private Stardust network.
# This is a free testnet that remains compatible with the iota-sdk 1.x (Stardust protocol).
# The public IOTA testnet (api.testnet.iotaledger.net) has been deprecated.

# IOTA Stardust node URL for Layer 1 operations (DID, proofs on Tangle)
NODE_URL="https://stardust.linksfoundation.com/node1"

# Faucet URL to request free test tokens for Layer 1 transactions
FAUCET_URL="https://stardust.linksfoundation.com/faucet/l1/api/enqueue"

# Block explorer URL for viewing transactions and outputs on the Tangle
EXPLORER_URL="https://stardust.linksfoundation.com/node3/dashboard/explorer"

# ------------------------------------------------------------------------------
# IOTA Layer 2 (EVM) Endpoints
# ------------------------------------------------------------------------------
# These endpoints connect to the LINKS Foundation's EVM-compatible chain
# running on top of the Stardust network.

# JSON-RPC endpoint for EVM smart contract interactions
RPC_PROVIDER="https://json-rpc.evm.stardust.linksfoundation.com/dtcb-chain"

# EVM chain ID for the LINKS Foundation testnet (used for transaction signing)
CHAIN_ID="1074"

# Deployed AssetFactory smart contract address on the L2 EVM chain
# This address is specific to the LINKS Foundation testnet deployment
ASSET_FACTORY_ADDR="0xA07AB84EE0D1c266728584B471b09B0b1Dfa4F6D"

# Private key for signing L2 EVM transactions
# WARNING: This is a testnet key. Never use real funds with this key.
# Override this in .env.trust.local for your own deployments.
L2_PRIVATE_KEY="ad86d1e354d004f291132df1555bd8dadfaf2665ce9c360f602447657fcce175"

# ------------------------------------------------------------------------------
# IOTA Wallet Storage (Layer 1)
# ------------------------------------------------------------------------------
# Configuration for the Stronghold-based wallet that manages L1 funds and keys.

# Password to encrypt/decrypt the Stronghold wallet snapshot
# WARNING: Use a strong password in production environments
STRONGHOLD_PASSWORD="some_hopefully_secure_password"

# BIP-39 mnemonic phrase for wallet recovery
# WARNING: This is a testnet mnemonic. Never use it to manage real funds!
MNEMONIC="quote adapt poet treat shadow tumble giant inmate tuna banana pass party solid ankle earth clutch broccoli auction morning item unusual friend include melt"

# Path to store the wallet database (account data, transaction history)
# This path is inside the Docker container volume
WALLET_DB_PATH="/var/lib/trust/walletdb"

# Path to store the Stronghold snapshot file (encrypted secrets)
# This path is inside the Docker container volume
STRONGHOLD_SNAPSHOT_PATH="/var/lib/trust/snapshot.stronghold"

# ------------------------------------------------------------------------------
# IOTA Identity Storage (DID Management)
# ------------------------------------------------------------------------------
# Separate Stronghold storage for managing Decentralized Identifiers (DIDs).

# Path to the identity key storage Stronghold snapshot
KEY_STORAGE_STRONGHOLD_SNAPSHOT_PATH="/var/lib/trust/key_storage.stronghold"

# Password for the identity key storage
KEY_STORAGE_STRONGHOLD_PASSWORD="some_hopefully_secure_password"

# BIP-39 mnemonic for identity key derivation
# WARNING: This is a testnet mnemonic. Never use it for production identities!
KEY_STORAGE_MNEMONIC="raise script athlete plastic stamp lion exhibit mention hint leopard curve gap parade adult surge large pizza claw unveil spy sorry industry salmon juice"

# ------------------------------------------------------------------------------
# Logging
# ------------------------------------------------------------------------------

# Filename for the application log file
LOG_FILE_NAME="dlog.log"
